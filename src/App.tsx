import React from 'react'

export default function App() {
  return (
    <div className="app">
      <header>
        <h1>Machine Client Log Summarizer</h1>
        <p>Simple Vite + React + TypeScript starter dashboard.</p>
      </header>

      <main>
        <section className="panel">
          <h2>Summary</h2>
          <p>No logs loaded yet — upload a file or connect a stream.</p>
        </section>
      </main>
    </div>
  )
}
